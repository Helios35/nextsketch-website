# Decision Log — NextSketch Website Rebuild

Running record of non-obvious calls and their rationale. Newest last. Sprint-scoped decisions live in the sprint plan; only decisions that outlive a sprint get promoted here.

**Numbering — read this before adding a row.** Numbers run in one unbroken sequence and are never renumbered, reused or reworded; a superseded row stays in place and says so. The log runs **#1–#44 with no gaps, so the next new row is #45.** Rows **#25 and #26 were written after the fact** (unit 24, 2026-08-28) at the numbers seven source files were already citing, reconstructed from the merged PR #30/#31 diffs and those doc blocks. They carry the dates the calls were made, not the date they were recorded — which is the convention for any reconstructed row.

| # | Date | Decision | Rationale / authorization |
|---|------|----------|---------------------------|
| 1 | 2026-06-11 | Fonts: **Inter + Caveat** approved. UX Spec Review Note 1 closed. **CLOSED — SUPERSEDED** by the 2026-06-14 pivot to **Space Grotesk + JetBrains Mono**; reconciled in the Sprint 03 doc audit (2026-06-22) — `docs/04-ux-spec.md` + `06-taxonomy.md` now canonicalize the new pairing and retire Inter/Caveat. | Owner approval during foundation unit; superseded by owner pivot. |
| 2 | 2026-06-11 | Repo `Helios35/nextsketch-website` flipped to **private**; brand `.docx` stay gitignored as defense in depth. | Doc stack contains internal strategy/messaging. Owner approval. |
| 3 | 2026-06-11 | Drafted copy **approved as canonical**: `SITE` title/description, `MODAL_OFF_RAMP` body + closing, `MODAL_SUCCESS.headline`, `MODAL_FAILURE`. | Owner approval at Sprint 01 planning; closes foundation build-notes deviations 4–5. |
| 4 | 2026-06-11 | Footer socials: **LinkedIn, X/Twitter, Instagram**. PRD Review Note 2 closed. URLs owed by owner before launch. | Owner call at Sprint 01 planning. |
| 5 | 2026-06-11 | Stat strip ships; **placeholder numbers** until owner supplies real stats before launch. PRD Review Note 1 closed (content still owed). **⚠ SUPERSEDED** by the 2026-06-14 pivot — the stat strip was dropped and repurposed into the dark hero's **capability strip** (four services, no invented numbers — Rule 4.3); stat-strip copy stays dormant (`HERO.stats`) for the deferred multi-section build. | Owner call at Sprint 01 planning; superseded by owner pivot. |
| 6 | 2026-06-11 | **Lead API deferred to Sprint 02** (with Resend account + Vercel setup, owner-owned). Modal interim submit = failure-fallback → email escape hatch. **CLOSED — shipped in Sprint 02 (see #7).** | Accounts don't exist; visual build doesn't depend on them. Owner call at Sprint 01 planning. |
| 7 | 2026-06-22 | **Lead-capture architecture (shipped Sprint 02), promoted from the sprint plan.** A serverless `POST /api/qualify` route re-validates each submission and hands it to a delivery seam: the **Google Sheet is the system of record and gates capture success**, **Asana is a best-effort task mirror**, and **Resend** sends the lead auto-reply + Nate's alert (best-effort, after the response). Two entry doors — a low-friction quick path and the full qualifier — plus an off-ramp that captures an email. Never reports "captured" unless the Sheet write succeeds (no fake success). | Sprint 02 shipped it (PRs #11–#15, build-notes 09–13); now the permanent capture architecture, so promoted here. Touches `src/app/api/qualify`, `src/lib/lead-*`, `qualification-modal.tsx`, `docs/05/07/08` (reconcile in Sprint 03). |
| 8 | 2026-06-22 | **No backend beyond that one serverless route — ever.** No database, no login/accounts, no standalone backend. The Google Sheet (+ Asana) is the system of record; `/api/qualify` is the entire server-side footprint. Anything that appears to need more stops and returns to the owner. | Owner decision ("we'll never need a login, so a backend is unnecessary"). Prevents scope creep into auth/DB; baked into brief 01 as a hard guardrail. Touches all future build scope. |
| 9 | 2026-06-22 | **Transactional email sends server-side via Resend — not via a Claude connector or a scheduled poll.** A scheduled Claude "new leads" digest is acceptable only as an optional owner-facing extra, never the lead-facing path. | The connector/scheduled task runs only while a session/job is active; the live site must send instantly and autonomously for any visitor, 24/7. Resend is server-side, instant, free at this volume. Re-litigated repeatedly this session; logged to settle it. Touches `src/lib/lead-notify.ts`. |
| 10 | 2026-06-22 | **Planning docs (briefs, sprint plans, context transfers) live in the Cowork workspace, not the code repo. Only code/deliverables go in the repo** — e.g. the Inbound-Leads Apps Script ships as `scripts/inbound-leads.gs`, not its brief. | Owner decision. Keeps the repo to shippable artifacts. Touches where future briefs/plans are saved. |
| 11 | 2026-06-22 | **Definition of "live" / the launch bar.** "Live" = the production **Vercel** deployment passing the lead-capture smoke test (both doors, end-to-end — `08-runbook.md`). The `nextsketch.com` **domain cutover** off Webflow is a **separate, parked milestone** (#5), **not** part of the "live" bar. | Decouples go-live from DNS; the Vercel production URL is the bar today (decision-log #5 parked). Owner direction. |
| 12 | 2026-06-22 | **Sprint 03 doc audit — reconciliation posture (docs follow the build).** The 2026-06-14 pivot is recorded as **as-built, not re-litigated**; every reconciled claim is tagged **CURRENT / CHANGED / DEFERRED**. The **multi-section content plan is DEFERRED, not deleted** (component code dormant, copy preserved in `src/content/copy.ts`). Color **token names** (paper/ink/white/accents) are **retained in code**; only the **theme** changed (dark: `ink` surface, `white` text, `gold` accent) — `paper` + lavender/rose/sage are deferred to the multi-section build. The quick door's optional input is an as-built **multi-select needs selector** (Sprint 03 adhoc), documented in `05`/`06`. Decision #1 formally closed here. *(The "DEFERRED multi-section" posture for the old light section plan and the non-gold accents is superseded by #13/#14.)* | Sprint 03 doc-audit brief; `sketch-doc-stack` (audit) + `sketch-scope-lock`. Reconciled on branch `docs/mvp-audit`. |
| 13 | 2026-07-06 | **Redesign reactivated — multi-section, in the dark hero language.** Five sections — **Manifesto, Services, Process, About, Final CTA** — will be added to the live single-page site, each designed **per-section, with the owner, against the hero-derived design system** (`04-ux-spec.md` v3.0). Unit 01 of the redesign is design-system-only (no section code). This supersedes the old eleven-section light plan **as a destination**: the retired paper/editorial/sketch design must not be referenced for new sections; its dormant component code/copy stays on disk (delete vs. keep remains a separate owner call — build-note 08). Sections not in this set (Selected Work, Testimonials, Who-it's-for, FAQ, nav/footer) are not planned; adding any back requires a new decision. | Owner decision (workspace redesign brief 01, 2026-07-06). Entry recorded during Redesign Unit 01 (the brief predates it citing this number). |
| 14 | 2026-07-06 | **Gold is the only accent — every other color orphaned.** The design system's single accent is the hero's `gold` `#E4B976` (with its binding `gold-ink` pairing). `lavender`/`rose`/`sage` (+ `-ink` pairs) and `paper-bright` are **orphaned**: token names remain in `globals.css` (removing them is a future owner-approved code change) but they have **no design role** and are unavailable to new sections. `paper` is orphaned from the system with one live exception — the 404 surface (code wins; re-skin is a possible future unit). The per-service / per-phase accent mappings (old Taxonomy §1–2) are retired. The 2026-07-06 inspiration image's rust/orange was explicitly **not** adopted. | Owner directive during Redesign Unit 01 ("keep the gold color used in the hero and orphan all other colors"). Codified in `04-ux-spec.md` v3.0; taxonomy/PRD aligned same unit. |
| 15 | 2026-07-07 | **Cinematic scroll-video backgrounds — hero orbit + persona sequence (Unit 03).** The site's backgrounds are owner-generated cinematic footage of Nate (Seedance 2.0 via Higgsfield, identity-referenced, brand-gold grade, consistent wardrobe): a 360° **orbit clip replaces the hero's interim Unsplash still** (closing the owner-owed swap) on a pinned 260vh runway where **scroll position is the camera angle** (Lando Norris / OFF+BRAND reference), and the single consulting-video backdrop becomes a **three-clip scroll-scrubbed sequence** (Strategist → Builder → Partner) crossfading across the below-hero range. The subtle green/red accents exist **in the footage only** — the UI token system stays gold-only (#14 unchanged). The owner instruction supersedes Unit 02's "don't touch the hero" guardrail for the hero *background and pinning structure only*; hero composition, modal, and lead pipeline remain untouched. | Owner directive (2026-07-07 chat instruction). As-built: build-note 18; spec reconciled in `04-ux-spec.md` v3.1. |

| 16 | 2026-08-24 | **`#work` reactivated as the proof band — the first conversion-recovery section.** Decision #13 held Selected Work dormant and required a new decision to bring it back; this is it. Visitors were reaching the site and leaving, and the owner's read is that nothing on the page demonstrated **proof of work or authority**. The section ships **directly below the hero** (proof before the argument), as an **opaque `bg-ink` band matching the footer** — the one section on the page that is not a transparent band over the footage, because the screenshots are its whole job and moving footage competed with them. It takes index **(01)**, renumbering Manifesto→(02), Services→(03), Process→(04), About→(05), Final CTA→(06), and leads `NAV.items`. Structure is adapted from an owner-supplied shadcn "gallery4" reference, which contributed **composition only**: **no new dependencies** (the reference needs embla/lucide/radix/cva; the rail is native CSS scroll-snap and the shared `ArrowIcon`), squared not `rounded-xl`, and the spec's **segmented gold meter** instead of the reference's dot pagination (§Interaction vocabulary ends "No dots."). Rule 4.3 holds: names, summaries, screenshots and links are **owner-owed**, and cards render the layout-final placeholder until they land. Pricing is the next section in this effort (still owed). | Owner directive (2026-08-24 chat instruction, adhoc — outside the brief set). As-built: build-note 20. Touches `04-ux-spec.md` (§Live components), `06-taxonomy.md` §6–7. |
| 17 | 2026-08-24 | **An opaque section must not cost the backdrop its frames.** `ScrollVideo` now maps from the bottom of the **hero-plus-contiguous-opaque region** (`[data-backdrop-hidden]`) rather than the hero alone, so `range` becomes `scrollHeight − opaqueBottom`: inserting an opaque band of height H grows both by H and the **frames-per-pixel cadence is unchanged to the pixel** (measured: 4440px with and without the band). The sequence still opens on clip one's **first** frame the instant the backdrop becomes visible and lands on clip three's **last** at page bottom, with no footage spent behind the band. The preload gate stays anchored to the hero release so its viewport of lead isn't spent inside the opaque wall. Contract for any future marked section: **directly below the hero, padding-spaced (no vertical margins), no transform on its root** — a non-contiguous opaque section needs a new approach, not this attribute. | Owner requirement (2026-08-24: "MAINTAINING scroll animations and scrolling cadence… the visuals currently begin and end at perfect frames"). Adversarially reviewed; as-built: build-note 20. |

| 18 | 2026-08-24 | **Hero headline replaced — "From idea to production. Gain a real partner."** Supersedes Messaging Kit §05 **Hero Option A** ("From idea to production. And we stay."), which `03-site-architecture.md` row 2 recorded as locked. The gold payoff words move with it, from "production" / "stay" to **"production" / "partner"**: `accentWords` is matched against the headline text, so leaving "stay" would have silently dropped the hero to one gold word. The retired line stays on disk in the dormant `HERO.headline` — superseded, not deleted. The live hero reads `LANDING.headline`. **The Messaging Kit still marks Option A as locked and lives outside the repo (claude.ai project doc) — owner-owned to reconcile.** | Owner directive (2026-08-24 chat instruction). A Rule 4.1 edit to canonical copy. As-built: build-note 20. Touches `01-vision.md`, `03-site-architecture.md`, `04-ux-spec.md` §Typography. |
| 19 | 2026-08-24 | **No em dashes in rendered site copy — standing rule, not a one-off edit.** Binds every user-facing string in `src/content/`, including sections not open when the rule was set. It drove the Services headline rewrite ("Software products and agentic systems **—** built for how your business actually works." → "Products and agentic systems**,** built for how your business actually works.") and the comma that replaced the em dash inside the Agentic Systems card description. **Doc blocks and code comments are exempt** — developer notes, not site copy. **Not enforced mechanically:** `check-banned-terms.mjs` has no em-dash scan, and adding one is a separate owner call (recommended in build-note 20). | Owner directive (2026-08-24: "Make sure to remove all em dashes from the specified sections"), generalised to a standing rule per the owner's framing. The Services headline is canonical §05 copy, so this is also a Rule 4.1 edit. As-built: build-note 20. |
| 20 | 2026-08-24 | **About body is owner-authored biography — and §3.4 gains its first exemption.** The section's DRAFT copy was written from documented positioning alone, so it could only restate claims the rest of the page already made; it is replaced by the owner's own biography. The opening paragraph contains **"I started in industrial design"**, which §3.4's retired-brand sweep bans by name, so the copy **failed the build as supplied**. That ban exists to stop the retired **service line** reappearing as a current offer; biography — where Nathan trained, not what NextSketch sells — is the opposite use. `scripts/check-banned-terms.mjs` therefore carries one sanctioned exemption, **scoped to `src/content/copy.ts` and asserted at exactly one occurrence**: the same clause in any other content file still fails, and rewording the paragraph fails loudly rather than leaving a dead bypass open. A fifth closing paragraph was added and then removed at the owner's direction; **relocating its "we stay" promise is an open owner call**, not settled here. | Owner-authored copy + owner directive (2026-08-24). §3.4 amended in this unit. Adversarial review caught an earlier global-strip version of the exemption that let a service claim pass; the scoped form is verified against a four-case matrix (build-note 20). |
| 21 | 2026-08-24 | **`#work` takes the first nav slot.** `NAV.items` leads with Work, ahead of Why / Services / Process / About. PR #27 shipped this recorded as the *builder's* judgment call ("drop this item and the section is still reachable by scrolling"); this row **ratifies it as the owner's decision**, so it is no longer a call a later agent may quietly reverse. Consistent with #16: a visitor who came for evidence should be one click from it. | Owner ratification (2026-08-24), promoting the builder's judgment call recorded in `src/content/copy.ts`. `06-taxonomy.md` §6 already reflects it. |
| 22 | 2026-08-25 | **The nav is a hamburger at every breakpoint; the visible desktop tab row is retired.** The bar is now the wordmark slot and one right-justified burger at all widths, and the full-screen ink overlay is the only place the items render. This is a change to shipped nav behavior, not a tweak: the anchor tabs were a documented component (`04-ux-spec.md` §Live components, "anchor tabs only, right-justified") and a visitor at desktop now needs one click to see any destination. The overlay's contract is unchanged (focus trap, Escape, scroll lock, focus return) and it takes the bar's gutter ladder so its lockup lands on the same gutter at every width. **Known narrowing, accepted:** the tab row was the nav's only no-JS surface, so with scripting off the bar is a wordmark and an inert burger; `SiteFooter` maps the same `NAV.items` as plain anchors, so all six destinations stay reachable. A `@media (scripting: none)` fallback row is the fix if that is not acceptable. | Owner directive (2026-08-25 chat instruction, adhoc — outside the brief set): "one hamburger button, right-justified, at every screen size." As-built: build-note 22. Touches `site-nav.tsx`, `04-ux-spec.md` §Live components, `03-site-architecture.md` §Navigation. |
| 23 | 2026-08-25 | **Pricing ships as a standalone route (`/pricing`), not a section.** Decision #16 named Pricing "the next section in this effort"; the owner's 2026-08-24 instruction **supersedes that framing** with a nav item plus a separate page. The site was documented throughout as single-page and one route, so this is a live architectural change, not a content addition. **A static route is not a backend: decision #8 is untouched** — `/pricing` prerenders to static HTML and `POST /api/qualify` remains the entire server-side footprint, with no database, no auth and no CMS. The consequence that actually bites is linking: every nav and footer href was a bare `#id` derived from a `SectionId`, and a bare hash resolves against the current route, so `#work` on `/pricing` means `/pricing#work` — nothing. `NAV.items` therefore carries a finished **root-relative** href per item, and both wordmark targets move to `/#top`. Unit 22 ships the route **layout-final and empty** (nav, footer, metadata, ink surface, heading and body saying the detail is coming); prices, tiers and bullets are Unit 23's and inventing them would breach Rule 4.3. | Owner directive (2026-08-24 chat instruction), superseding the "section" framing in #16. As-built: build-note 22. Touches `src/app/pricing/`, `src/content/pricing.ts`, `NAV.items`, `03-site-architecture.md` §Sitemap, `06-taxonomy.md` §6, `07-technical-spec.md`. |
| 24 | 2026-08-25 | **Pricing takes the last nav slot.** Order is Work → Why → Services → Process → About → **Pricing**. Work keeps the first slot, ratified by #21 and not reopened here. Pricing goes last because it is the only item that leaves the page and the one a visitor looks for on purpose: at the end of the list it reads as a destination rather than an interruption in the middle of the page order, which is what putting a route between two anchors would do. | Owner call (2026-08-25), set in the same instruction as #22–#23. Recorded rather than left as a builder judgment call, so it is not a call a later agent may quietly reverse — the same posture #21 took for Work. |
| 25 | 2026-08-25 | **The pricing tier structure, the term, and the tier names.** `/pricing` carries **four tiers in this order**: **AI Workflow Integration** ($3,998 upfront, struck from $5,000), **Internal Tool** ($7,998, struck from $10,000), **Save Your Project** (upfront quoted), **Custom Product** (quoted). Every tier carries the **$298 monthly partnership**, and **the term is said out loud on every card and once above the grid** — a price that hides a commitment is exactly the "surprise invoice" Brand Philosophy §6 rejects, so it is not tucked into a footnote. The term is **required for the first three months, optional after**, reduced mid-build from an initial twelve; **Save Your Project carries no required term at all** — a two month trial with no contractual obligation, because that tier's visitor has already been burned by one firm and asking them to commit before seeing anything work is the wrong ask. The page-level sentence therefore **names that exception** rather than stating a blanket commitment its own card contradicts. **Naming:** the first tier's working name ended in the term Rule 3.2 bans and Brand Philosophy §8 calls out by name, so it ships as **"AI Workflow Integration"** and its copy says "embedded agents"; "AI Tool" became **"Internal Tool"** (a platform the client owns and runs internally), slug and modal preselect unchanged. Tiers 1–2 are **owner-authored scope** and deliberately diverge from Brand Philosophy §4 — §4 is the authority on voice here, not on scope. **The page sells nothing and takes no payment: it qualifies.** Every tier CTA opens the qualification modal with that tier's need preselected (`PRICING_NEED`): `workflow` → `agentic` · `tool` → `new_product` · `rescue` → `rescue` · `custom` → `partnership`. **That mapping is recorded here as as-built only.** The content doc block argues the last two are deliberate rather than mistaken; that reasoning is the builder's, stated in a code comment, and **no owner direction on record confirms it** — so this row records what ships without ratifying why. **#8 is untouched.** **Tier feature bullets are owner-owed (Rule 4.3) and ship empty** — each card renders no list rather than an invented one. | Owner directive (2026-08-25 chat instruction, adhoc — outside the brief set), revised by the owner in-session the same day: the term 12 → 3 months, tiers 1–2 rescoped and renamed, struck former prices added. Copy revised again **2026-08-26** (PR #31): the rescue tier's upfront reads "Quoted" rather than "No upfront", and Custom Product names a flat rate and a 6–8 week delivery window. **Row reconstructed 2026-08-28 (unit 24) from the merged PR #30/#31 diffs and the seven source files that already cite this number** — recorded at the number the code points at, not a new call. As-built: build-note 23. Touches `src/content/pricing.ts`, `src/components/pricing-tiers.tsx`, `src/lib/types.ts` (`PricingTier`, `PricingTierSlug`), `src/components/modal-trigger.tsx` (`need`), `src/app/pricing/page.tsx`, `04-ux-spec.md` §`/pricing`. **Superseded in one field only, 2026-09-09: the monthly is $2,098, not $298 (#38).** Everything else in this row stands, and the $298 stays written here because the row records the call made on 2026-08-25, not what the page charges today. |
| 26 | 2026-08-25 | **The nav bar carries one button beside the burger — `NAV.featured`, which is Pricing.** The bar is the wordmark slot on the left, and the button plus the burger on the right, at every width. This **narrowly supersedes the 2026-07-06 "no CTA in the nav" direction**: that call was about *conversion* — the hero CTA and `#start` carry it, and a third conversion button was redundant — and this is *navigation to a page*. So it takes §Interaction vocabulary's de-emphasized **`ghost`** variant rather than the hero's white divided-arrow advance, and **the nav still has no conversion CTA**. Pricing keeps its menu row, so the button is a shortcut and not the only way there; bar and menu read the **same object**, so the two can never drift. The button **hides below 375px** (the smoke-test width), where the bar measurably stops fitting — the menu carries Pricing at every width regardless. Two consequences shipped with it and are binding: `<Button>` gained a **`size` prop**, whose `compact` value paints 38px and taps 44px via a transparent `::after`, so §Layout's binding touch target survives a control deliberately shorter than it (**`min-h-11` therefore lives in the size table, not the base class — every size that is not hit-extended must carry it**); and the bar became **`pointer-events-none` with `pointer-events-auto` restored on its own controls**, which is what let `/pricing` link its own above-the-fold lockup to `/#top`. Unit 22 shipped that lockup un-linked because the fixed bar spanned the top band at `z-40` and swallowed every click on it (#23); a page can now put a real link under the transparent bar. | Owner directive (2026-08-25 chat instruction, adhoc — outside the brief set), refined by the owner in-session the same day: the compact button was thinned from a responsive step to a flat 38px bar variant. **Row reconstructed 2026-08-28 (unit 24) from PR #30 and the source files that already cite this number** — recorded at the number the code points at, not a new call. As-built: build-note 23. Touches `src/content/copy.ts` (`NAV.featured`), `src/components/site-nav.tsx`, `src/components/button.tsx`, `src/app/pricing/page.tsx`, `04-ux-spec.md` §Nav. |
| 27 | 2026-08-28 | **One service vocabulary, everywhere a visitor can read it.** The four services render as **New Product**, **Product Completion**, **Product Support** and **Agentic System** on the Services cards, the hero capability strip and the `/pricing` tiers. **The modal's quick-door labels are the reference**, because the quick door is the only surface a visitor reaches *after* choosing: a name it does not use is a name that disappears mid-conversion, and a visitor who picked "Ongoing Partnership" on a card was then asked to choose "Product support" for the same thing. This **closes the 2026-08-04 divergence**, where the cards took short names, Taxonomy §1 and `LANDING.capabilities` kept long ones, and the split was flagged rather than resolved. **The service slugs are renamed with the names** (`rescue` → `product-completion`, `partnership` → `product-support`, `agentic` → `agentic-system`; `new-product` unchanged), so a slug is never a fossil of a retired name — they are what unit 26's routes will be built on. **Two `/pricing` tiers are renamed onto the same set**: "Save Your Project" → **Product Completion**, "Custom Product" → **New Product**. Nothing else about pricing moves — not a price, a struck former price, a description, a note, the card order, or the empty `features` arrays. **The stored vocabulary does not move**: `PROJECT_TYPE_VALUES`, the modal and the `/api/qualify` payload are untouched, so `product-support` still stores as `partnership` and no lead data migrates. The **pricing tier slugs are deliberately not renamed**, leaving `custom` carrying "New Product" and `rescue` carrying "Product Completion" — recorded as a separate owner call, not resolved here. | Owner directive (2026-08-28, adhoc unit 25). Closes the divergence flagged in `src/content/services.ts` since 2026-08-04 and in build-note 21's sweep. As-built: build-note 25. Touches `src/content/services.ts`, `src/content/copy.ts`, `src/content/pricing.ts`, `src/lib/types.ts` (`ServiceSlug`), `06-taxonomy.md` §1 and §9. |
| 28 | 2026-08-28 | **The hero capability strip is aligned to the quick door's names — a Rule 4.1 edit to Messaging Kit §05 canonical copy.** `LANDING.capabilities` rendered the long §05 forms ("New Products from Scratch", "Rescue & Completion", "Agentic Systems Integration", "Ongoing Product Partnership") while the cards rendered short ones and the modal rendered a third set, so the first thing a visitor read on the site was a vocabulary nothing else used. It now renders the same four names as everything else (#27). **This is recorded separately from #27 because it is an edit to canonical copy, not a naming cleanup** — the same posture #18 took for the hero headline and #19 for the Services headline. **Messaging Kit §05 still carries the long forms and lives outside this repo, owner-owned to reconcile; do not "correct" the strip back to them.** The strip is a marquee with room for the long forms, so this is not a layout fix: it is the vocabulary decision reaching the one surface that had been exempted from it. | Owner directive (2026-08-28), authorized by the unit 25 brief, which required this be recorded as the Rule 4.1 edit it is rather than folded into #27. As-built: build-note 25. Touches `src/content/copy.ts` (`LANDING.capabilities`), `05-business-rules.md` §4.1, `06-taxonomy.md` §1. |
| 29 | 2026-08-28 | **Two `/pricing` tier CTAs preselected the wrong project type — corrected. A shipped bug, not a change of mind.** `tool` ("Internal Tool") opened the modal on *New product* and `custom` (now "New Product") opened it on *Product support*; a visitor clicked a tier and landed on an option that did not match what they had just chosen. `PRICING_NEED` now reads `workflow` → `agentic` · `tool` → `agentic` · `rescue` → `rescue` · `custom` → `new_product`. **The doc block above the mapping is replaced, not just its values.** It previously argued the `custom` mapping was deliberate and told the reader it was "the mapping a reader is most likely to assume is a mistake and is not" — a comment that pre-emptively warns the next reader off a correction is what kept the bug alive for three days. #25 records the old mapping **as as-built only** and explicitly never ratified that reasoning, which is what left this correctable. **The top two tiers share `agentic` on purpose**: they differ in *depth*, not category. AI Workflow Integration is agents and custom tools dropped into processes the business already runs, at workflow level with no product wrapped around them; Internal Tool is a full product with a real interface and agents behind it, owned by the client and logged into by their team. The modal asks what a visitor needs, not how deep they want to go — the depth is what the two price points express. That distinction is **positioning language and unit 26 depends on it**, so it lives in Taxonomy §1, not only in a code comment. | Owner correction (2026-08-28) plus the positioning in the unit 25 brief's outcome 6. As-built: build-note 25. Touches `src/content/pricing.ts` (`PRICING_NEED` and its doc block), `06-taxonomy.md` §1. |

| 30 | 2026-08-28 | **The service routes ship as *two* grouped pages, not four, and the home-page service cards click through to them.** `/services/product` carries **New Product**, **Product Completion** and **Product Support** as anchored blocks; `/services/agentic-system` carries the **two depths** #29 records (**AI Workflow Integration**, **Internal Tool**). The unit-26 brief specified four thin routes, one per `ServiceSlug`; the owner reduced that to two, each covering a related group, because the three product services differ in *entry point* rather than in what is delivered, and the agentic service is one thing sold at two depths. **A service page slug is therefore not a `ServiceSlug`** — `product` is a group with no service behind it, while `agentic-system` is spelled exactly like its slug because that page is that service. The brief also left *how the cards reach the pages* open as an owner call, and this closes it: **the card's name is now a link** to that service's route, and the card's existing CTA still opens the modal on that service, so the conversion seam is unchanged and the page gains a second, quieter affordance. **CHANGED 2026-08-30:** the three product cards originally carried block anchors, per this row's "goes to the sections covering each topic clicked". That was settled before the routes had a hero worth landing on — measured, a card put the visitor at scrollY 544 with the `<h1>` 199px above the fold, skipping the ribbon, the heading, the CTAs and the graphic. All four cards now open their route **at the top**. The block anchors remain real `<section id>` targets for a shared or bookmarked URL; nothing on the site generates one. There is **no `/services` index route** — the home page's `#services` section is the hub. **Page structure is owner-specified and is four blocks: hero, the anchored topic blocks, how it works, close.** No proof band, no FAQ, no testimonials, and **no price on the page**: `/pricing` is untouched, and the pricing link the close briefly carried was **removed by the owner** (2026-08-28) so the close carries one action. Each block's CTA is `<ServiceCta>`, **the same gold text link the home page's service cards carry** (owner direction, same date), replacing the reference's outline button so a service's CTA looks identical wherever a visitor meets it. The "how it works" band is the four canonical phases (Taxonomy §2) as a numbered card grid, adapted from an owner-supplied reference that contributed **layout only** — the PR #27 / build-note 23 posture. **The "what you get" band is alternating split rows, built against a third owner-supplied reference** (same session; it went glass cards → flat columns → this, and the first two are scrapped). Each block is a two-column row — text on one side, a **wireframe mock** on the other — and **the sides alternate down the page**. **The mock is purpose-built per block**, not one motif reskinned five times (owner feedback, same day, after the first cut gave every block the same tile cluster with the marks reordered): a product UI, a developer workspace, a live-product ops panel, a dashboard UI, and the reference's tile cluster kept for AI Workflow Integration, the one block it suits. Layout only, again: the reference is shadcn and **none of its dependencies ship** (`Card`, `Button`, `lucide-react`, `@radix-ui/react-slot`, `class-variance-authority`, `cn()`, a `/components/ui` directory — nothing installed, per the standing zero-new-dependencies posture and "icons are inline SVG, never `lucide-react`"). Its **third-party product logos are refused outright**: rendering GitHub, Slack and Notion would assert integrations nobody approved, which is the invented claim Rule 4.3 exists to stop, on the pages a search visitor lands on first. **No mock contains a readable string, a numeral, a metric or a logo**, and every one is `aria-hidden` — that is the Rule 4.3 requirement, not a style choice, and decision #5's retired stat strip is the precedent. They are built only from shapes the system already uses. The mocks are a **flagged judgment call** — §Interaction vocabulary settles the *list marker* and bans lucide but does not cover a decorative wireframe, and the spec's own instruction for that case is to generalize and flag. Because the rows are tall, arriving via a card **highlights the row you arrived at**: its index and its emphasized tile's hairline go gold, CSS `:target` only, which is the Process accordion's open-row treatment doing the same work. **The deliverable bullets ship as DRAFT** rather than empty — the owner asked for the layout built, and a layout with nothing in it cannot be reviewed; they are the ones drafted in build-note 26 and remain the owner's to edit. **What is still owner-owed and therefore renders nothing:** the one-line promise `<h1>` per page (the heading falls back to the canonical name rather than a drafted placeholder) and the grouped page's hero intro. | Owner direction, 2026-08-28, in session, superseding the unit-26 brief's four-route shape and answering its two open questions. Rule 4.3 governs the empty slots; the two page titles and descriptions are DRAFT on the `SITE.title` precedent (#3). |

| 31 | 2026-08-28 | **`rose`, `sage` and `lavender` are readable again — in exactly one place. A narrow supersession of #14.** The service routes' "what you get" wireframes (`service-block-visual.tsx`) carry **interface-chrome colour**: syntax highlighting in the IDE mock, status dots in the dashboard and ops mocks, an avatar chip, tinted marks in the tile cluster. Owner direction: "add more colour to the skeletons that are in the same colour range as our palette (reds greens and others)". **#14 is narrowed, not reversed**, the way #26 narrowed "no CTA in the nav": (a) the colours are **interface chrome inside a decorative mock**, never a brand accent and never on a real control; (b) **no block owns a colour** — the per-service `accent` field in `src/content/services.ts` is still unread and still dead data, and the retired accent-block card vocabulary stays retired; (c) **`gold` remains the only emphasis**, spent at full strength once per mock while the three sit at 45–85% alpha beneath it; (d) **no new token** — #14's "no new tokens" clause is untouched. The alphas and the 1.75px mark stroke were **measured in-browser, not guessed**: `sage` and `lavender` are deliberately desaturated pastels and resolve to grey below roughly 70% alpha or 1.5px of stroke on a near-black tile, and start competing with gold above 85%. Recorded as its own row rather than folded into #30 because #14 is a shipped, cross-cutting decision and a later agent reading it must find this. | Owner direction, 2026-08-28, in session, restated with specifics after the first pass. Narrowed by the builder and flagged; `04-ux-spec.md` §Orphaned colors is amended to match. |

| 32 | 2026-08-30 | **The service heroes take the reference's structure: the shared ribbon, a two-up split, a Pricing second CTA, and a tilted wireframe illustration.** Owner-supplied reference hero, adapted to the system. Four calls inside it. **(a) The capability strip becomes a shared component.** It was inline in `hero.tsx`; the owner asked for *the same* ribbon on the service heroes rather than a second one, which requires one component with two call sites. Extracted verbatim to `capability-strip.tsx` with `items` / `label` / `copies` props — `/`'s rendered markup is **byte-identical**, verified before and after. Each service route passes its own canonical names. **(b) The `hero` type scale was considered and rejected**; the heading stays at `display`, because the one-line promise is still owner-owed and the canonical-name fallback would dominate at `lg:text-8xl`. **(c) The second CTA is Pricing** — the shared `<Button>` at `ghost`, unfilled, default size, reading `NAV.featured` so it cannot drift from the nav bar's button (#26). It is navigation, not conversion, so Rule 3.1's exhaustive set is untouched. **(d) Perspective enters the system, flagged.** The repo had no `perspective`, `rotateX`, `preserve-3d` or `skew` anywhere — the only rotation was the `rotate-45` gold diamond — so a tilted plane is genuinely new vocabulary rather than a reuse. Recorded rather than slipped in: §Surfaces and §Interaction vocabulary do not cover a decorative tilted plane, and the spec's own instruction for an uncovered case is to generalize and flag. Everything *on* the plane is existing vocabulary and only the tilt is new; a static transform is layout, not motion, so no keyframe was added and `globals.css` is untouched. The reference's readable node labels are refused under Rule 4.3 (owner-confirmed: skeleton bars only), and the illustration is contained and faded rather than bled off the page, because a rotated plane overflowing horizontally widens the page and the usual fix would sit above the `sticky` lockup and risk the 80px handoff. | Owner direction, 2026-08-30, with the type scale, the second CTA and the label question settled by the owner before build. Scope was explicitly the hero only; `hero.tsx` is the one file outside it that changed, and only to import the extracted strip. |

| 33 | 2026-08-31 | **The elevated-surface value becomes a real token, and the service-route rows become cards on it.** Two calls in one owner directive ("the background token should be consistent... only need to be changed in one location... we should not be adjusting this on a case by case basis"). **(a) `#0a0a0c` is promoted from a per-component literal to `--color-surface`,** defined once in `globals.css` and consumed as `bg-surface` / `bg-surface/95` by every card, panel, mock frame and glass surface — the pricing tiers, the process cards, the home services cards, the work-rail cards, the qualification modal, the nav bar and overlay, and the service-route mock frames. §Color's "one deliberate literal sits outside the token set" carve-out is closed; the *value* is unchanged everywhere, verified by computed-background fingerprint on all four pages before and after. The declaration is **`@theme inline`, load-bearing**: utilities inline the raw hex, so `bg-surface/95` statically folds to the exact color the old literals compiled to — in the plain `@theme` block it compiles a runtime `color-mix()` over a `var()`, which lands one RGB step off the build-time fold (measured: green 10 vs 9 over black). This narrows #14's "no new tokens" clause the way #26 and #31 narrowed their rules: no new *color* enters the system — an existing shipped value gains a name so it stops being copy-pasted. **(b) Each "what you get" row on the service routes is now a card** — the site's card recipe exactly (`border-white/15`, `bg-surface`, `p-6 md:p-8`, the `white/30` hover lift), solid rather than glass because no footage is mounted there — so a block's text and its mock read as one unit from desktop to the stacked mobile column. `VisualFade`'s far stop follows the fill it now sits on (`ink` → `surface`), else every mock would ring itself with a slightly darker vignette inside its card. **A pre-existing mobile fault surfaced and is fixed in the same change:** the mocks' fixed-pixel chrome, capped by the shell's `max-w-md`, forced the stacked mobile grid track to 448px, so both routes really rendered 497px wide at a 375px viewport and mobile Chrome zoomed the page out to hide it — unit 26's `scrollWidth === innerWidth` check could not see it because auto-zoom equalizes both sides, and its build note records the pane refusing to render below 466px. `min-w-0` on the row's two columns (the bento tiles' own load-bearing device, one level up) lets the track shrink; the mocks' `overflow-hidden` stages clip and the shared fade dissolves the clip. Verified at a real 375 via headless-Chrome device metrics: both routes now lay out at exactly 375. | Owner direction, 2026-08-31, in session (unit 27). Scope: visually, only the service-route rows change; the token sweep is rendering-inert everywhere else, proven by identical computed-background tallies on `/`, `/pricing` and both service routes. `04-ux-spec.md` §Color and §Surfaces reconciled. |

| 34 | 2026-08-31 | **The AI Workflow Integration visual carries real app icons — a narrow supersession of #30's logo refusal, for this one visual only.** Owner direction: "We need to have actual app icons like the behance and linkedin icons in the footer... add dotted lines like they are all connected... keep the focus on the center icon. Make the center icon anthropics logo or chat gpt with emails and other services being connected to it." The abstract tile cluster becomes a **hub-and-spoke app cluster**: the **Anthropic logomark** at the centre in gold on the emphasized tile (the owner offered Anthropic or ChatGPT; the OpenAI path is a one-line swap in `APP_ICONS`), six satellites — an email envelope, LinkedIn, Slack, GitHub, a calendar, Google — spread around it with **dotted square-cap spokes** tying each to the hub. Icon sources follow the footer's own precedent (`social-icon.tsx`): satellites are **Bootstrap Icons** (MIT, the matched 16-unit set the footer already ships, including the same LinkedIn mark); the centre is **Simple Icons**' Anthropic mark, which Bootstrap does not carry. Nothing was installed — inline SVG paths, per the standing posture. **What #30 refused was rendering logos *nobody had approved*; the owner approving these is the condition that refusal was waiting on.** The narrowing holds everywhere else: the other four mocks still carry no logo, no readable string, no numeral; `gold` still appears once (the hub); the #31 chrome tones are reassigned to the satellites unchanged; the visual is still `aria-hidden`; the fade, the 4/3 stage, the rise-in assembly (wiring first, then the hub, then the apps) and the `:target` gold hairline on the centre tile all persist. The fade now rides the stage rather than the cluster's own box — correct, not a regression: the spread layout fills the stage the way the framed mocks fill theirs, which is the condition build-note 26's "fade on the cluster's own box" trap was about. | Owner direction, 2026-08-31, in session (unit 27), with the screenshot of the old cluster attached. Scope: this one visual; the rest of the site untouched. `04-ux-spec.md` §`/services/*` reconciled. |

| 35 | 2026-08-31 | **`/pricing` and the two service routes take a fixed gold page-background glow — the home page explicitly not.** Owner-supplied background component (a radial-spotlight + animated noise-canvas + grid snippet), with the instruction "ONLY use the glow NOTHING ELSE. The glow should be our color amber. DO NOT TOUCH THE HOME PAGE." Shipped as `src/components/page-glow.tsx`: the source's exact spotlight geometry (`circle 560px at 50% 200px`), viewport-fixed at `-z-10`, recoloured from its `#f97316` orange to the brand `gold` (rgb(228 185 118), a literal for the `::backdrop`/`VisualFade` gradient-paint reason) **tempered to 0.3 alpha** — the source ships its colour at full strength, which floods the hero and costs the white headline its contrast; the alpha is the declared intensity knob. **Dropped, per "only the glow":** the noise canvas (the snippet's only client JS — an ungated per-frame rAF canvas repaint the §Motion posture has no slot for; without it the component is server-rendered and ships zero JS), the demo's grid lines, and the `bg-slate-950`/`bg-black` bases (the pages are `ink`; `slate` does not compile here). The snippet's shadcn `/components/ui` instruction was not followed — components live flat in `src/components/` (build-note 23; unit 26 refused the same directory). **Integration:** the three routes' own bands shed their `bg-ink` (`service-page.tsx` ×3, `service-process.tsx`, `pricing/page.tsx`, `pricing-tiers.tsx` — all page-scoped) because an opaque band over a `-z-10` backdrop hides it; the pages' ground is the layout's `ink`. This narrows §Surfaces' "no glows" rule the way #26/#31/#34 narrowed theirs: it governed *component depth* (shadow and blur, never colored light) and still does — this is a page backdrop, owner-directed. The ambient gold hero glow removed 2026-08-30 (build-note 26) was a band-local gradient inside the service hero; this supersedes that removal with a different, owner-supplied treatment at page scope. Home verified untouched: no glow node in its DOM, footage mounted, all its `bg-ink` bands intact. | Owner direction, 2026-08-31, in session (unit 27), component pasted in chat. `04-ux-spec.md` §Surfaces, §`/pricing` and §`/services/*` reconciled. |

| 36 | 2026-08-31 | **The two agentic depths are re-split on the custom-tool line, and their canonical descriptions carry it.** Owner definition: **AI Workflow Integration** is agentic work that needs **no custom tool** — Claude or OpenAI integrated into what already runs, handling day-to-day tasks (checking emails, running reports); **Internal Tool** always means a **custom tool**, with custom agents where the job demands them (the CrewAI/LlamaIndex class of build, or an internal operations or marketing dashboard Claude plugs into). The two `PRICING_TIERS` descriptions are rewritten to carry the split — a Rule 4.1 canonical-copy edit, owner-directed: `workflow` now reads "Claude or OpenAI agents integrated into the work your business already does: checking emails, running reports, moving tasks along. No custom tool to build, no new platform to learn." and `tool` reads "A custom tool built for your specific business processes, from a dashboard Claude plugs into to purpose-built agent crews. A real tool that you own, built around how your team actually works." ("Agent crews" is the deliberate vendor-neutral nod to the CrewAI/LlamaIndex class; the owner's examples are recorded here, not rendered.) **Because the service-page blocks *reference* the tier strings (#30's single-source doctrine), the two agentic `/pricing` cards updated in lockstep** — the same offer now reads identically everywhere it is described, which is the doctrine working as designed; de-sharing would have drifted the same offer into two versions. Prices, tier names, notes and the other two tiers are untouched. The blocks' DRAFT bullets are reframed on the same line: 01 loses "Custom tools where an agent alone will not do the job" (that is now 02's business) and gains the Claude/OpenAI integration and everyday-tasks bullets; 02 gains "A custom tool…" and "Custom agent crews where the job demands them". Rule 3.2 note: the owner's word for 01 was "automations", which is a banned term — the idea ships as the named tasks instead. | Owner definition, 2026-08-31, in session (unit 27): "01 is agentic workflows that do NOT require a custom tool… 02 … definitely require a custom tool." Scope: agentic offering content only; nothing visual changed. |

| 37 | 2026-08-31 | **Five messaging upgrades from the avolis.ai comparison ship on the agentic offering** (owner: "ship all of it"). The owner supplied https://www.avolis.ai/ as a Google-ranking competitor and asked for their messaging that beats ours while staying inside our framing; the comparison is recorded in build-note 27 §6. Approved and shipped: **(1a)** the agentic page's one-line promise — `headline: "We find where agents pay off, build them in, and stay."`, `accentPhrase: "stay"` — closing the `<h1>` fallback that had rendered the canonical name since #30 (the **product** page's promise is still owner-owed); **(2)** "actually pay off" replaces "create real value" in the §05 Agentic System line (canonical, Rule 4.1 — renders on the home card) and in block 01's first bullet; **(3+4)** the agentic hero intro becomes bespoke — problem, approach, honesty: "Most teams guessing at AI buy the wrong tools and stall in pilots that never ship. We assess where agents actually pay off, then build them into the work itself. If agents will not pay off in your business, we will tell you." — no longer the §05 reference, which keeps its home-card job; **(5)** block 01's closer becomes "nothing new to babysit" (tier description, so the `/pricing` card follows in lockstep, as #36). Explicitly **not** adopted from the reference: testimonials and invented metrics (§10, #5), 30–60–90-day claims (real numbers or nothing, Rule 4.3), the "AI transformation" umbrella, a FAQ band (#30), industry verticals, and the "automation" vocabulary (Rule 3.2). **Owner-owed follow-up:** the Messaging Kit's §05 copy (outside the repo) needs reconciling to the new line. | Owner approval, 2026-08-31, in session (unit 27): "1a, 2, 3, 4, 5.. ship all of it". Verified: banned-terms clean on a fresh build; the accent renders gold on "stay". |

| 38 | 2026-09-09 | **The ongoing monthly partnership is $2,098, replacing $298.** Owner call, 2026-09-09. The figure changes on the three tiers that name one — **AI Workflow Integration** (`workflow`), **Internal Tool** (`tool`) and **Product Completion** (`rescue`) — and **New Product** (`custom`) still reads "Quoted monthly". **Nothing else in #25 moves:** the upfront figures ($3,998 / $7,998 / Quoted / Quoted), the struck former prices, the tier names, the tier order, the empty `features` arrays and every CTA preselect (`PRICING_NEED`, as corrected by #29) are untouched. **The term is unchanged, and so is its wording:** required for the first three months on `workflow`, `tool` and `custom`, optional after; **Product Completion still carries no required term** — a two month trial with no contractual obligation — and the page-level sentence still names that exception rather than stating a blanket commitment its own card contradicts. At the new figure the required term is **$6,294 committed**, which makes saying it out loud matter more, not less: the sentence was re-read against Brand Philosophy §6 and left word for word as it shipped, because it was already plain and a bigger number is not a reason to soften it. **No strikethrough on the old monthly.** The struck figures on the first two cards are *upfront* prices framed as a discount; striking $298 beside $2,098 would advertise a price increase, so it is deliberately not done. **No new tier, no reordering, no card redesign, and no new accent** — gold stays the only one (#14). **#8 is untouched:** the page still sells nothing and takes no payment, and `/api/qualify` remains the entire server-side footprint. **The monthly figure in #25 is superseded by this row; the rest of #25 stands.** | Owner directive, 2026-09-09, delivered as an adhoc brief (outside the brief set) and authorized in session. As-built: build-note 28. Touches `src/content/pricing.ts` (three `ongoing` values, `description`, `term`, and the doc blocks that cite the figure), `src/lib/types.ts` (the `ongoing` JSDoc example), `src/components/pricing-tiers.tsx` (the doc block explaining the refused billing toggle, which also had the term wrong at "first year" and now reads "first three months" per #25), and this log (this row, the #25 supersession note, the numbering preamble, the open-items list). Tier feature bullets are still owner-owed and still ship empty (Rule 4.3) — no value bullets were invented to justify the figure. No component, layout, route or tier-shape change. |
| 39 | 2026-09-12 | **Case studies ship as their own routes: a `/work` grid plus `/work/<slug>` per study.** The site was four routes (#23, #30); it is now those four plus `/work` plus **one dynamic segment prerendered per case study at build** from content (`generateStaticParams` over `CASE_STUDIES`; `dynamicParams` is `false`, so an unknown slug returns the site's own 404 without a render). **Static routes are not a backend — decision #8 is untouched:** every case study page prerenders to HTML, sells nothing, and `/api/qualify` remains the entire server-side footprint. Two things change on the home page and nothing else: the proof band's **"View all" control lands on `/work`** instead of the owner's Behance profile (that URL stays in the footer socials), and **every work card lands on its own case study route** instead of the project's Behance page — the Behance link is kept in content as `sourceHref` and rendered on the study's route as a link out, so the published page is one hop away rather than gone. The grid tile is **the existing work card in a `tile` variant** — same file, reached through a prop, no fork — and the home band's markup is otherwise identical (measured: the built home HTML differs from the previous build in the five destinations and nothing else, and the backdrop range at 1920×1080 is still 4532px with the same boundary frames, #17). Unit 24 ships both routes **layout-final and empty**: nav, footer, metadata, the ink surface under the page glow, the study's name and summary, and body copy saying the detail is coming. **No narrative, results, metrics, client names or quotes** (Rule 4.3); the template that fills the room is Unit 25's, built against a reference the owner supplies. | Owner decision (2026-09-12, adhoc brief "Units 24 + 25", outside the brief set). As-built: build-note 29. Touches `src/app/work/`, `src/components/work-grid.tsx`, `work-rail.tsx` (the exported card and its variant), `src/content/work.ts`, `src/content/case-studies/`, `src/lib/types.ts` (`CaseStudy`, `ROUTES.work`, `workHref`, `WorkItem.slug` and `sourceHref`), `03-site-architecture.md` §Sitemap, `04-ux-spec.md` §Live components, `06-taxonomy.md` §6–7, `07-technical-spec.md`. |
| 40 | 2026-09-12 | **The nav's Work item continues to mean the home page proof band (`/#work`). `/work` is reached from the band's own control, not from the nav.** Recorded because the two now sit one character apart in the URL bar and a future reader will assume one is a mistake. Neither is: `#work` is the section (#16, #21) — the first thing a visitor who came for evidence should reach — and `/work` is the archive behind its "View all". #21 and #24 are untouched, the item set stays six, and the nav is deliberately not route-aware. Adding `/work` (or the case study routes) to `NAV.items` is a separate owner call, the same posture #30 took for the service routes. | Owner decision (2026-09-12), settled in the brief and explicitly not the builder's to revisit. `src/content/copy.ts` is untouched — that is the point. Recorded in `03-site-architecture.md` §Navigation and `06-taxonomy.md` §6. |
| 41 | 2026-09-12 | **No CMS. Case study content and assets live in the repo.** Content is typed, **one module per case study** under `src/content/case-studies/`, collected in one ordered list that the home band, the `/work` grid and the case study routes all read — so adding a study is adding a content file and its images, never a page, route or component edit. Imagery lives under the existing public asset convention (`/public/work/`, Taxonomy §7); **anything heavy enough to bloat the repo — video, large image sequences — is hosted externally and referenced by URL, and is never committed into `public/`.** Recorded against the facts rather than the brief's premise: the brief cited the scroll footage as the precedent for external hosting, but `hero-orbit.mp4` and the three backdrop clips are committed under `public/` (12.3 MB, unit 03). They stay where they are; this rule binds case study media going forward, and moving the footage is a separate owner call. Recorded so the question stops being reopened: the site has no backend (#8), and content-as-code is what makes the banned-terms gate and Rule 4.1's owner-approval trail work at all. | Owner decision (2026-09-12). As-built: build-note 29. Touches `src/content/case-studies/`, `06-taxonomy.md` §7, `07-technical-spec.md` §System overview. |
| 42 | 2026-09-18 | **The first case study template (Unit 25 v1) is rejected and archived; Unit 25 restarts from the grid.** On review of the rendered routes the owner threw the design out, so the seven commits that built it on 2026-09-14 — the reference-page clone adapted to the design system, its types and block vocabulary, the DRAFT copy, the badge row, the imagery, the decision rows numbered #42 and #43 there, and build-note 30 — live unmerged on `archive/case-study-template-v1`, and `adhoc/case-study-template` is cut fresh at the grid tip (#39–#41, build-note 29), source byte-identical to it. **Nothing from v1 is carried over until the owner sets the new direction**, and this row is the one place that says so, so the next builder neither rebuilds v1 nor inherits it by accident. What survives v1 as reference, to be pulled by file — never by commit — once the rebuild needs it: the owner's renders, already renamed into `public/work/mascot/` (six, the floating handhelds first) and `public/work/agentic-platform/` (six, the laptop first, `UI Intro` last); the Platform badge vocabulary the owner fixed — **Web App · SaaS · Mobile App · Device · Agentic Platform · Internal Tool**, one study can carry more than one; the `CloseBand` extraction of the service routes' close (byte-identical HTML, so it holds under any design); and the owner's imagery rules — only the files in the folder the owner names, the block layout never moves for a study, the frames keep their ratios and the image crops to the frame, a spare slot stays a placeholder and is reported. The archive's #42 and #43 are not a numbering conflict: that branch never merges, so on this lineage the sequence runs #41 → #42 here. | Owner decision (2026-09-18): the pages as landed are rejected outright ("throw those changes out"); fresh branch rather than reverts, archive rather than delete, both on the builder's recommendation. The grid (Unit 24) is explicitly fine and untouched. This branch carries no source change yet; the rebuild's docs and build-note come with the rebuild. |
| 43 | 2026-09-18 | **The case study template, take two: the owner's own composition, on the design system, Mascot first.** The owner supplied two direction images, a Volvo Cars App case study page as inspiration and his own rough Mascot adaptation of it on ink, and a folder of thirteen transparent renders. `/work/mascot` is that composition built on the system: a header row (name at the panel scale, the ratified summary, squared mono chips, the off-site links right) and his headline at the hero scale; two device renders whose lower parts sink behind an elevated panel, the mascot render stepping out over the panel's edge on the page's one `Parallax`; the panel (a render beside the summary, a body and three bullets, then his "What did we do?" centred and answered with the proof band's own line and the six on-device screens); an annotated hardware band, the owner's eight baked-in labels rebuilt as mono micro-labels on hairline leaders ending in the gold marker, plus the front-and-back render; a closing band with the parent-app render; the other studies as tiles; the service routes' close. **Renders are cut-outs laid out by their measured visible box** (`CaseStudyRenderImage`: transparent margins trimmed, no opaque pixel cut, never stretched, `next/image` never upscaling), not screenshots in frames. **The page is named slots in content** (`CaseStudyPageContent`, declared beside the study's card in its own module and registered by slug in `index.ts`, deliberately not a field on the card object so no page's copy rides the rail's or the grid's client payload), and a study without a page renders the #39 placeholder unchanged: the other three do today. Adaptations from the references, each flagged: the white card is the system's elevated card (`paper`/white are orphaned as section surfaces; `--shadow-modal` is the depth token §Surfaces allows); the inspiration's big numerals, day-by-day timeline, inline headline chip, corner status labels and callout cards are not reproduced, because each would assert a figure or a schedule nobody approved (Rule 4.3); band headings are centred (a new `align` prop on `SectionHeading`, the owner centred every heading); content sits on the service routes' `max-w-6xl` measure, generalized from the 2026-08-30 direction. The close is a verbatim copy of the service routes' inline close (`close-band.tsx`); switching `service-page.tsx` to it is a zero-render-change refactor deferred because the owner scoped this unit to the Mascot page. | Owner direction (2026-09-18): "Use the inspiration example and the example I created as direction. Make adjustments as you see fit. Same rules apply: our design system should be used, our component tokens, systems and messaging. Create the Mascot case study ONLY. NOTHING else should be touched." Composition his, style the system's, the posture of #30 and #39. Copy is DRAFT in `src/content/case-studies/mascot.ts`, nowhere else, and claims only the ratified summary, the proof line, his own strings and what the renders show. As-built: build-note 30. Touches `04-ux-spec.md` §/work, `06-taxonomy.md` §7, `03-site-architecture.md`, `07-technical-spec.md`. |
| 44 | 2026-09-18 | **A case study's chip row is its service plus one or more platform badges from a fixed vocabulary, and "Visit website" renders only for a live project.** The badge vocabulary is the owner's (2026-09-14, set on the archived first template and re-recorded here because that branch never merges): **Web App · SaaS · Mobile App · Device · Agentic Platform · Internal Tool**, six values, a study carries one or more, labels written once in `PLATFORM_BADGES` (`src/content/work.ts`) and keyed by `PlatformBadge`. The chip is the system's squared hairline mono badge, never a pill. The first chip is the study's service by its canonical Taxonomy §1 name, resolved from a `service` slug that also derives the close's `need` through `SERVICE_NEED`, so there is one mapping. The owner's example chips were "Product Development" and "AI Application": the first is New Product in the site's vocabulary; the second is not in the badge set and is not rendered (Mascot carries Device and Mobile App), a one-string change if he wants it. **"Visit website"** (`liveHref`) is the owner's live-only rule from 2026-09-14: none of the four studies is live, so the control appears nowhere and the string ships unused on purpose; "View on Behance" renders whenever a study has a published page. Both are the §Interaction-vocabulary gold text link with the shared arrow turned outward, navigation rather than conversion, so Rule 3.1 does not bind their labels. | Owner direction (2026-09-14, badges and the live-only rule; 2026-09-18, the example's chips). Canonical in `src/lib/types.ts` (`PlatformBadge`, `CaseStudyPageContent.service` / `badges` / `liveHref`) and `src/content/work.ts` (`PLATFORM_BADGES`, `CASE_STUDY_PAGE.visit`). Recorded in `06-taxonomy.md` §6. |

**Still open (owner):** **the Mascot page's copy, DRAFT pending ratification (#43, build-note 30): the headline (his own line, set in sentence case with a full stop and a comma; his casing is a one-string revert), the panel's eyebrow, body and three bullets, the hardware and parent-app bands' eyebrows, headings and lines, the six screen captions, the front-and-back caption, and every alt, all in `src/content/case-studies/mascot.ts`; the "AI Application" chip from his example, not rendered because it is outside the badge vocabulary (#44)** · **three sharper exports owed for the two slots that outrun their files (#43): `Robot.png` at 540px wide or more, `Home Device.png` and `Favorites Device.png` at about 900px wide, same transparent cut-outs, same folder; every other render sits at or under its file's width** · **the three unused files in the Mascot folder, reported rather than placed: `Home Device-1.png` and `-2.png` (the home screen with the icons spaced slightly differently) and `Device Physical UX_words.png` (its labels are rebuilt as callouts; windowing its sharper device out of the baked labels would cut opaque pixels, which the imagery rule forbids without the owner's word)** · **the firm's process on a case study: not built (#43); `<ServiceProcess />` can be mounted between the closing band and Other projects unchanged if wanted** · **the panel's surface: built as the system's elevated card; a white version is one class swap (`bg-paper text-ink`) with the inner text ladder and the gold markers re-derived on a light ground** · **the content measure on the case study page (#43): the Other projects tiles stop at the measure while `/work`'s grid runs to the gutters; one constant to reverse** · **`service-page.tsx` still carries its inline close; switching it to `close-band.tsx` is a zero-render-change refactor for a unit that may touch the service routes** · **the other three studies' pages: SaaS Platform, Agentic Platform and Parcell render the #39 placeholder until the owner supplies their folders, one study at a time** · **`/work` and case study route copy, DRAFT pending ratification (#39, build-note 29): `WORK_PAGE.title` / `description`; the grid's reuse of the proof band's own eyebrow, headline and supporting line as its `<h1>` and intro (a page-specific line is the owner's to write); `CASE_STUDY_PAGE` (the "Case study" eyebrow, the "full case study is coming" line, "All work", "View on Behance"); and the four studies' `title` / `description` metadata strings — all in `src/content/work.ts` and `src/content/case-studies/`, nowhere else** · **the case study slugs as shipped — `mascot` · `saas-platform` · `agentic-platform` · `parcell`, the published titles slugified (#39; Taxonomy §6) — ratify, or rename one field per module** · **the home cards' Behance destination moved one hop (#39): the published page is a "View on Behance" link on each case study route rather than the card itself; confirm, or say the word to put the direct link back on the cards** · **the gold page glow (#35) on `/work` and the case study routes — shipped so every route without the footage reads as one family; one line per route to remove if #35 should stay at its three** · **`NOT_FOUND.body` still says "NextSketch lives on a single page now" — stale since #23, now the landing for `/work/<unknown>` (#39), and one of the em dashes counted below; DRAFT copy, the owner's to rewrite** · **`WORK_INTRO` renders the brand as "Next Sketch" (two words, owner wording) against Taxonomy §8's "NextSketch", now on `/work` and, since #43, on `/work/mascot` under "What did we do?" as well as the band (build-note 20 deviation 2, still open)** · **Service-route copy — the `/services/product` one-line promise `<h1>`, still rendering the canonical name until it lands (the agentic page's landed 2026-08-31, #37); plus the deliverable bullets, which ship as DRAFT and want ratifying or rewriting (the two agentic blocks' were reframed under #36/#37 and are still DRAFT). **The `/services/product` hero intro is closed**: drafted in build-note 26 against the gap Rule 4.3 left open, and owner-approved 2026-08-30, so it is canonical copy rather than DRAFT (#30; all of it lives in `src/content/service-pages.ts` and nowhere else)** · **the two service-page titles and descriptions, DRAFT pending ratification (#30)** · **Tier feature bullets — empty on all four `/pricing` cards (#25; Rule 4.3, drafted and awaiting approval; the component already lays the list out, so approved bullets drop into `src/content/pricing.ts` and nowhere else)** · **a line saying what the $2,098 monthly partnership actually buys — the page states the price and the term but never the deliverable (#25, still open at the figure #38 set; Brand Philosophy §6 has the language)** · **the struck former prices ($5,000 / $10,000) are a standing discount claim (#25) — fine if those are genuine prior or list prices, but a discount that never expires stops reading as one against the brand's no-hype posture** · **the pricing tier slugs (`workflow` · `tool` · `rescue` · `custom`) do not map one-for-one onto Taxonomy §1's service slugs, and `src/lib/types.ts` cites `06-taxonomy.md` for a tier table that document does not carry (#25) — recorded here, deliberately not resolved** · **a no-JS fallback row for the nav, if the #22 narrowing is not acceptable (recommended, not built)** · **Messaging Kit §05 still marks Hero Option A as locked — reconcile to #18 (owner-owned, outside the repo)** · **relocating the "we stay" promise dropped with the About closing paragraph (#20)** · **an em-dash scan for `check-banned-terms.mjs` to enforce #19 mechanically (recommended, not built) — measured 2026-08-28 with doc blocks and comments stripped: 35 rendered strings across `src/content/`, one em dash each. `modal.ts` 13 · `email.ts` 11 · `copy.ts` 8 (`SITE.title`, `SITE.description`, `NOT_FOUND.body`, two `PROCESS` phase descriptions, plus dormant `HERO`, `TESTIMONIALS`, `FIT`) · `faq.ts` 3 · `pricing.ts` / `services.ts` / `work.ts` 0. **18 are on live surfaces**, 11 are transactional email, 6 are dormant copy. Unchanged by PRs #30/#31. #19 and the shipped copy disagree today; whether #19 is enforced or narrowed is the owner's call and nothing was edited** · Asana task creation not yet working live (verify `ASANA_ACCESS_TOKEN` / `ASANA_PROJECT_ID` + redeploy) · draft copy approval (email + quick-door + off-ramp, `src/content/*`) · off-ramp asks name+email (confirm, or make email-only) · Resend sending-domain verification (`LEAD_FROM_EMAIL`) · DNS cutover off Webflow (parked, #5/#11) · full production smoke test (both doors, end-to-end) · OG/social share image (interim; the self-hosted hero background asset is delivered — #15).

**Resolved (2026-08-31, unit 27):** **The inert `[animation-delay:…]` class form on `/` and `/pricing` is fixed** — the four elements moved to the inline `style={{ animationDelay }}` form the service hero and `ScrollReveal` already use, and the documented 0 / 120 / 200ms entrance stagger now computes (measured before and after with `getComputedStyle`; reduced motion re-verified under a forced `prefers-reduced-motion: reduce`). The site no longer carries the broken pairing anywhere.

**Resolved (2026-08-28, unit 24):** **Pricing page content — the four tiers, their figures and the monthly term shipped 2026-08-25/26 in PRs #30/#31 and are recorded as #25; the tier feature bullets are the one part still owed and stay open above.** `/pricing` is no longer layout-final and empty.

**Resolved this session:** Resend account created · Vercel env vars set · live Sheet capture verified · **Sprint 03 doc audit reconciled** (docs/00–08 + this log; on `docs/mvp-audit`, pending review/merge — #12).
